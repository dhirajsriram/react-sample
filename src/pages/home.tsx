import React from 'react';
import Loader from '../common/loader/loader';

const Home: React.FC = () => {
  return (
    <div className="home-container">
      <Loader></Loader>
    </div>
  );
}

export default Home;
